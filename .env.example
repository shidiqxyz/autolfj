# Your Wallet Private Key (Without 0x prefix or with it, script handles both)
PRIVATE_KEY=your_private_key_here

# Monad RPC URL (Default provided in script, but can be overridden)
RPC_URL=https://rpc.monad.xyz
